{
  "name": "jokari-api",
  "version": "1.0.0",
  "description": "Jokari Club Zürich — backend API (Express + Firestore)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.1",
    "cors": "^2.8.5",
    "@google-cloud/firestore": "^7.10.0"
  }
}
